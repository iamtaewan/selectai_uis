"""HTTP 라우터 패키지 — 경로/메서드/모델의 단일 기준은 docs/api-spec.md §13 인덱스.

스캐폴딩 규칙(병렬 작업 충돌 방지):
- 모든 라우터/경로는 스캐폴딩 단계에서 스텁(501)으로 선등록되어 있다.
- 구현 에이전트는 배정된 라우터 파일의 함수 본문만 채운다.
  (경로·메서드·시그니처·response_model 변경 금지, main.py 수정 금지)
"""
